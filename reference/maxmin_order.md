# Max-min ordering of points (Guinness 2018): greedily pick the point farthest (in min-distance) from those already chosen. O(n^2) pure R; fine to a few thousand.

Max-min ordering of points (Guinness 2018): greedily pick the point
farthest (in min-distance) from those already chosen. O(n^2) pure R;
fine to a few thousand.

## Usage

``` r
maxmin_order(coords)
```
