# Smoothed-density weight (kernel at the residual), used in the Newton Hessian and the sandwich variance. Read off the *fitted smoothed* density, never an ALD stand-in (the f(0) lesson from mixqr).

Smoothed-density weight (kernel at the residual), used in the Newton
Hessian and the sandwich variance. Read off the *fitted smoothed*
density, never an ALD stand-in (the f(0) lesson from mixqr).

## Usage

``` r
k_smooth(u, h)
```
