# Ordering-covariate slope surface (n x G): beta_const + B' theta per regime.

Ordering-covariate slope surface (n x G): beta_const + B' theta per
regime.

## Usage

``` r
slope_surface_matrix(beta, des, B, order_var)
```
