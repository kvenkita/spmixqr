# Broadcast the CAR phi surface (L x G) to new observations (n x G) via their unit.

Broadcast the CAR phi surface (L x G) to new observations (n x G) via
their unit.

## Usage

``` r
predict_car_phi(object, newcoords)
```
