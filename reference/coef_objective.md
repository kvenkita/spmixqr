# Smoothed penalised objective value for one regime (for tests and the EM objective).

Smoothed penalised objective value for one regime (for tests and the EM
objective).

## Usage

``` r
coef_objective(b, Xt, y, tau, w, Pen, h)
```
