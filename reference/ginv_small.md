# Minimal Moore-Penrose pseudo-inverse (avoids a MASS dependency). Vendored from mixqrgate's MASS_ginv.

Minimal Moore-Penrose pseudo-inverse (avoids a MASS dependency).
Vendored from mixqrgate's MASS_ginv.

## Usage

``` r
ginv_small(A, tol = 1e-10)
```
