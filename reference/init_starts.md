# A list of `nstart` initial responsibility matrices cycling through start types.

A list of `nstart` initial responsibility matrices cycling through start
types.

## Usage

``` r
init_starts(y, X, coords, G, tau, nstart)
```
