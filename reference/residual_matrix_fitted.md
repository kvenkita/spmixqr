# Residual matrix (response minus fitted quantiles), one column per regime. `fitted` is an n by G matrix of fitted quantiles.

Residual matrix (response minus fitted quantiles), one column per
regime. `fitted` is an n by G matrix of fitted quantiles.

## Usage

``` r
residual_matrix_fitted(y, fitted)
```
