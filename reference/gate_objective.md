# Penalised gate objective Q(gamma) (for testing the Newton step against optim).

Penalised gate objective Q(gamma) (for testing the Newton step against
optim).

## Usage

``` r
gate_objective(gamma, Z, P, Pen)
```
