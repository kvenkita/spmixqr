# Numeric floor for densities / probabilities (avoid log(0)).

Numeric floor for densities / probabilities (avoid log(0)).

## Usage

``` r
.dens_floor
```

## Format

An object of class `numeric` of length 1.
