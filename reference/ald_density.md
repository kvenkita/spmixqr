# Asymmetric-Laplace density at the tau-th quantile parametrisation (Yu & Moyeed 2001), scale `sigma`. Vendored from the mixqr ALD idiom.

Asymmetric-Laplace density at the tau-th quantile parametrisation (Yu &
Moyeed 2001), scale `sigma`. Vendored from the mixqr ALD idiom.

## Usage

``` r
ald_density(u, sigma, tau)
```
