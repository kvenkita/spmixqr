% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{absorb_car_constraint}
\alias{absorb_car_constraint}
\title{Constraint-absorbed incidence and precision for the CAR block.}
\usage{
absorb_car_constraint(R, Q, membership, eps = 1e-06)
}
\arguments{
\item{R}{Full unit-incidence matrix \verb{n x L} (\code{R[i, s(i)] = 1}), sparse.}

\item{Q}{Full \verb{L x L} CAR precision from \code{\link[=make_car_precision]{make_car_precision()}}.}

\item{membership}{Length-L connected-component labels (from \code{\link[=components_from_W]{components_from_W()}}).}

\item{eps}{Ridge added to the reduced precision.}
}
\value{
A list with \code{Rt} (constraint-absorbed incidence, sparse \verb{n x L'}), \code{Qt}
(reduced precision, sparse \verb{L' x L'}), \code{Tmat} (\verb{L x L'} recovery basis), and
\code{Lp} (\verb{L'}).
}
\description{
Imposes a per-connected-component sum-to-zero constraint on \code{phi} (so \code{beta}'s
intercept carries the level and \code{phi} is the mean-zero spatial deviation), absorbing
the constraint into a reduced incidence basis \code{Rt} (\verb{n x L'}, \verb{L' = L - n_comp}) and
reduced precision \code{Qt} (\verb{L' x L'}), analogous to mgcv's \code{absorb.cons}. A tiny
epsilon-ridge is added to \code{Qt} for conditioning. The CAR effect on the original \code{L}
units is recovered as \code{phi = Tmat \%*\% phi_reduced}, where \code{Tmat} (\verb{L x L'}) is the
constraint null-space basis stored in the return value.
}
\keyword{internal}
