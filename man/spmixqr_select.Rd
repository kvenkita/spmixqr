% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{spmixqr_select}
\alias{spmixqr_select}
\title{Select regimes and smoothing parameters}
\usage{
spmixqr_select(
  formula,
  data,
  coords = NULL,
  areal = NULL,
  tau = 0.5,
  gating = ~1,
  spatial_gate = TRUE,
  spatial_coef = TRUE,
  spatial_error = FALSE,
  spatial_W = NULL,
  car = c("proper", "icar"),
  car_alpha = 0.95,
  spatial_plus = FALSE,
  spatial_plus_k = NULL,
  method = c("ald", "kde"),
  G_grid = 2:3,
  lambda_gate_grid = c(0.1, 1, 10),
  lambda_coef_grid = c(0.1, 1, 10),
  lambda_error_grid = c(0.1, 1, 10),
  criterion = c("bic", "cv"),
  folds = 5L,
  weights = NULL,
  weights_type = c("sampling", "frequency", "precision"),
  control = spmixqr_control()
)
}
\arguments{
\item{formula, data, coords, areal, tau, gating, spatial_gate, spatial_coef, method}{Passed to \code{\link[=spmixqr]{spmixqr()}}.}

\item{spatial_error, spatial_W, car, car_alpha}{CAR spatial-error settings passed to
\code{\link[=spmixqr]{spmixqr()}}. When \code{spatial_error = TRUE} the \code{lambda_error_grid} is searched
(BIC, with the CAR-effective-df term) and \code{spatial_gate} is forced off (the
guardrail); \code{car_alpha} is fixed (not selected by the check loss).}

\item{spatial_plus, spatial_plus_k}{Spatial+ confounding-safeguard settings passed to
\code{\link[=spmixqr]{spmixqr()}} (the residualisation is refit within each CV training fold, so there is
no leakage). NNGP \code{range} selection is not searched here (use \code{criterion = "bic"}
comparing manually-built NNGP weights objects).}

\item{G_grid}{Candidate regime counts.}

\item{lambda_gate_grid, lambda_coef_grid, lambda_error_grid}{Candidate penalties.}

\item{criterion}{\code{"bic"} or \code{"cv"}.}

\item{folds}{Number of CV folds (for \code{criterion = "cv"}).}

\item{weights}{Optional observation weights: a length-\code{n} non-negative numeric
vector, the name of a column in \code{data}, or a one-sided formula \code{~w}. \code{NULL}
(default) is unweighted. Distinct from \code{spatial_W} (the CAR neighbour matrix).
Internally normalized to mean 1; point estimates are scale-invariant.}

\item{weights_type}{Interpretation of \code{weights}, selecting the variance
estimator (point estimates are identical across types): \code{"sampling"}
(inverse-inclusion / survey; design-robust SEs, default), \code{"frequency"}
(integer counts; \code{n_eff = Σw}), or \code{"precision"} (analytic \code{1/Var} weights).}

\item{control}{A \code{\link[=spmixqr_control]{spmixqr_control()}} list.}
}
\value{
A list with the best fit, the chosen settings, and the score table.
}
\description{
Fits \link{spmixqr} over a grid of \code{G} and/or roughness penalties and returns the best
by BIC (post-hoc unsmoothed log-likelihood plus effective df) or by K-fold
held-out predictive check loss. For the KDE density path the log-likelihood is
unavailable, so \code{criterion = "cv"} (the check-loss surrogate) is used.
}
