% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_spmixqr}
\alias{sim_spmixqr}
\title{Simulate from a spatial quantile mixture model}
\usage{
sim_spmixqr(
  n = 200L,
  G = 2L,
  tau = 0.5,
  gate_slope = 2.5,
  coef_slope = 1.5,
  sep = 3,
  sd = 0.7,
  crossing = FALSE,
  error = c("normal", "ald"),
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations / distinct locations.}

\item{G}{Number of regimes (2 or 3 supported).}

\item{tau}{Quantile level.}

\item{gate_slope}{Strength of the spatial gate (0 = constant gate; negative
control uses 0).}

\item{coef_slope}{Strength of the spatial slope variation (0 = flat slopes).}

\item{sep}{Regime separation (intercept gap).}

\item{sd}{Error scale.}

\item{crossing}{If \code{TRUE}, make slope surfaces cross in space (stresses labels).}

\item{error}{\code{"normal"} or \code{"ald"} component error.}

\item{seed}{Optional seed.}
}
\value{
A list with \code{data} (y, x, x2), \code{coords} (n x 2), and \code{truth}.
}
\description{
Generates point (or areal) data from a two-regime spatial mixture of quantile
regressions: a smooth spatial gate and spatially varying component slopes (à la
Reich, Fuentes & Dunson 2011, eq. 17, inside a mixture). Used for the validation
study. The conditional \code{tau}-quantile is exact by construction (the error's
\code{tau}-quantile is subtracted).
}
\examples{
d <- sim_spmixqr(n = 150, seed = 1)
str(d$truth)
}
