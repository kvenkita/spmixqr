% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis.R
\name{spmixqr_basis}
\alias{spmixqr_basis}
\title{Build a spatial basis and its roughness penalty}
\usage{
spmixqr_basis(
  coords,
  areal = NULL,
  type = c("tp", "gp", "mrf"),
  k = 20L,
  scale_coords = TRUE
)
}
\arguments{
\item{coords}{For point data, a two-column numeric matrix/data frame of
coordinates (length n). For areal data, a length-n factor/vector of region
labels (each observation's region).}

\item{areal}{For areal data, an \pkg{spdep} \code{nb} or \code{listw} neighbour object over
the unique regions (converted to mgcv's named-list form internally). \code{NULL} for
point data.}

\item{type}{Basis type; ignored (forced to \code{"mrf"}) when \code{areal} is supplied.}

\item{k}{Basis dimension (clamped to unique locations/regions minus one).}

\item{scale_coords}{Standardise point coordinates before the build.}
}
\value{
An object of class \code{spmixqr_basis}: the fitted smooth \code{sm}, the basis
matrix \code{B}, the penalty \code{Omega}, the coordinate transform, and metadata.
}
\description{
Constructs a low-rank spatial basis \code{B(s)} (sum-to-zero constrained) and an
associated PSD roughness penalty \code{Omega} using \pkg{mgcv}. For point data a
thin-plate (\code{"tp"}) or low-rank Gaussian-process (\code{"gp"}) smooth; for areal data a
Markov-random-field (\code{"mrf"}) smooth from a neighbour graph.
}
