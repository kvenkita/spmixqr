% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{nc_sids}
\alias{nc_sids}
\title{North Carolina SIDS counts (areal census example)}
\format{
A data frame with 100 rows (counties) and 9 variables:
\describe{
\item{county}{County name (the spatial unit id; matches \code{nc_sids_W} row/col names).}
\item{births}{Live births, 1974--78 (\code{BIR74}).}
\item{sid}{SIDS deaths, 1974--78 (\code{SID74}).}
\item{nwbirths}{Non-white live births, 1974--78 (\code{NWBIR74}).}
\item{east, north}{County-centroid longitude / latitude.}
\item{sids_rate}{SIDS per 1000 births (\code{1000 * (sid + 0.5) / (births + 1)}).}
\item{log_sids}{\code{log(sids_rate)} -- the modelled response.}
\item{pnw}{Proportion of non-white births (\code{nwbirths / (births + 1)}).}
}
}
\source{
The \code{nc.shp} North Carolina SIDS shapefile distributed with the \pkg{sf}
package (originally Cressie & Read 1985; Cressie 1993, \emph{Statistics for
Spatial Data}). Built offline from \code{sf::st_read(system.file("shape/nc.shp", package = "sf"))} and redistributed as a built-in for reproducible examples; see
\code{data-raw/make_nc_tracts.R}.
}
\usage{
nc_sids
}
\description{
Sudden-infant-death (SIDS) counts and live births for the 100 counties of North
Carolina, 1974--78, the canonical areal/lattice spatial dataset. Shipped as a tidy
data frame with county centroids and a derived log SIDS rate, to illustrate the CAR
spatial-error quantile regression on contiguous (polygon) areal units. The matching
queen-contiguity weights are in \link{nc_sids_W}.
}
\examples{
data(nc_sids)
data(nc_sids_W)
summary(nc_sids$log_sids)
}
\keyword{datasets}
