% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mstep-coef.R
\name{pen_smooth_wqr_car}
\alias{pen_smooth_wqr_car}
\title{Sparse penalised smoothed weighted quantile regression for the CAR design.}
\usage{
pen_smooth_wqr_car(
  Xt,
  y,
  tau,
  w,
  Pen,
  h,
  floor_abs = 0.001,
  beta_init = NULL,
  maxit = 50L,
  tol = 1e-07,
  dense_max = 40L
)
}
\arguments{
\item{Xt}{Augmented component design, a sparse \code{Matrix} \verb{n x P} (\verb{P = p + L'}).}

\item{y}{Response.}

\item{tau}{Quantile level.}

\item{w}{Observation weights (responsibilities) length n.}

\item{Pen}{\verb{P x P} sparse penalty matrix (PSD; zero on the beta block).}

\item{h}{Bandwidth rate.}

\item{floor_abs}{Absolute minimum bandwidth.}

\item{beta_init}{Optional warm start.}

\item{maxit, tol}{Newton iterations / tolerance per annealing stage.}

\item{dense_max}{If \code{P <= dense_max}, fall back to the dense solver path.}
}
\value{
list(beta, hessian, fitted, h, converged) (beta a plain numeric vector).
}
\description{
Identical annealed penalised-Newton logic to \code{\link[=pen_smooth_wqr]{pen_smooth_wqr()}}, but the augmented
design \verb{[X, Rt]} (with \code{Rt} the constraint-absorbed CAR incidence) and the penalty
block are stored as \pkg{Matrix} sparse objects, and the \verb{(p + L')^2} Newton system
is assembled and solved sparsely via \code{\link[Matrix:solve-methods]{Matrix::solve()}} / a sparse Cholesky. The
dense \code{\link[=pen_smooth_wqr]{pen_smooth_wqr()}} stays the non-CAR path; this solver is the CAR path
(\code{spatial_error = TRUE}). A dense fallback is used for tiny \verb{L'}.
}
\keyword{internal}
