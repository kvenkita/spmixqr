% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_plus.R
\name{spatial_plus_residualize}
\alias{spatial_plus_residualize}
\title{Residualise covariates against a spatial smooth (Spatial+ stage 1).}
\usage{
spatial_plus_residualize(X, slope_idx, geo, k = NULL)
}
\arguments{
\item{X}{The component design matrix (with intercept).}

\item{slope_idx}{Integer indices of the non-intercept columns to residualise.}

\item{geo}{The resolved geography (\code{resolve_coords()} output): point \code{coords} or areal
\code{region} + \code{areal} nb.}

\item{k}{Basis dimension for the smooth (\code{NULL} = a generous default richer than the
spatial-error resolution).}
}
\value{
A list with \code{X} (residualised design), \code{smooths} (named list of fitted \code{gam}s,
one per residualised covariate; \code{NULL} for skipped constant columns), and \code{spatialR2}
(a data frame of the spatial R-squared removed per covariate).
}
\description{
For each non-intercept column of the component design, regress it on a spatial smooth
(a thin-plate smooth of point coordinates, or a Markov-random-field smooth over areal
regions) and replace it by the residual. Returns the residualised design, the fitted
smooths (needed to residualise \code{newdata} at prediction), and the per-covariate spatial
R-squared removed.
}
\references{
Dupont, Wood & Augustin (2022, Biometrics).
}
\keyword{internal}
