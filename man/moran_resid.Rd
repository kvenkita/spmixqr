% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{moran_resid}
\alias{moran_resid}
\title{Permutation Moran's I on the spatial-unit residuals of an spmixqr fit}
\usage{
moran_resid(object, spatial_W = NULL, nsim = 999L)
}
\arguments{
\item{object}{A fitted \link{spmixqr} object.}

\item{spatial_W}{A \code{\link[=spq_weights]{spq_weights()}} object or weights matrix. Defaults to the fit's
CAR weights (\code{object$car$W}) when present; otherwise required.}

\item{nsim}{Number of permutations for the p-value.}
}
\value{
A list of class \code{spq_moran} with \code{before}/\code{after} (each a list with
\code{statistic}, \code{p_value}, \code{n_units}), or a single \code{statistic}/\code{p_value} for a
non-CAR fit.
}
\description{
Computes Moran's I of the responsibility-weighted quantile residual aggregated to
the spatial unit, with a permutation p-value (random relabelling of units). For a
CAR fit (\code{spatial_error = TRUE}) it reports the residual Moran's I \emph{after} the CAR
term and, by re-fitting without it, the value \emph{before} - the negative-control /
power diagnostic. For a non-spatial-error fit it reports the single residual Moran's
I against the supplied weights.
}
\details{
The mixture residual is \verb{r_s = sum_\{i in s\} sum_k p_ik (y_i - x_i'beta_k - phi_\{k,s\}) / n_s}, the responsibility-weighted residual aggregated to unit \code{s} (per-regime
residuals are ambiguous off-support; documented in the spec).
}
\examples{
\donttest{
set.seed(1)
d <- sim_spmixqr(n = 200, G = 1, tau = 0.5, spatial_error = TRUE,
                 lattice = 6, seed = 1)
fit <- spmixqr(y ~ x, d$data, coords = d$region, G = 1, tau = 0.5,
               spatial_error = TRUE, spatial_coef = FALSE, spatial_W = d$spatial_W,
               variance = "none", control = spmixqr_control(nstart = 1L, seed = 1))
moran_resid(fit, nsim = 199)
}
}
\references{
\pkg{spdep} permutation Moran's I (Bivand et al.); Cliff & Ord (1981).
}
