% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mstep-coef.R
\name{pen_smooth_wqr}
\alias{pen_smooth_wqr}
\title{Penalised smoothed weighted quantile regression (one regime's component step).}
\usage{
pen_smooth_wqr(
  Xt,
  y,
  tau,
  w,
  Pen,
  h,
  floor_abs = 0.001,
  beta_init = NULL,
  maxit = 50L,
  tol = 1e-07
)
}
\arguments{
\item{Xt}{Augmented component design \verb{n x P} (\verb{[1, x_j, x_j*B(s)]}, j>=2).}

\item{y}{Response.}

\item{tau}{Quantile level.}

\item{w}{Observation weights (responsibilities) length n.}

\item{Pen}{\verb{P x P} penalty matrix (PSD).}

\item{h}{Bandwidth \emph{rate} (dimensionless); the absolute bandwidth is this rate
times the residual scale, floored at \code{floor_abs}.}

\item{floor_abs}{Absolute minimum bandwidth.}

\item{beta_init}{Optional warm start (ignored if \code{NULL} or all-zero).}

\item{maxit, tol}{Newton iterations / tolerance per annealing stage.}
}
\value{
list(beta, hessian, fitted, h (absolute bandwidth used), converged).
}
\description{
Solves the penalised smoothed objective by penalised Newton with \strong{bandwidth
annealing} (a wide bandwidth is reduced geometrically to the target, warm-
starting each stage) and step-halving. Annealing is essential: at a tiny target
bandwidth the kernel Hessian is near-singular and undamped Newton diverges
(verified). At convergence the solution approximates the target-bandwidth
minimiser, and as the target bandwidth shrinks it approaches exact weighted
quantile regression.
}
\keyword{internal}
