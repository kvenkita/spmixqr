% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{spmixqr_control}
\alias{spmixqr_control}
\title{Control parameters for \code{\link[=spmixqr]{spmixqr()}}}
\usage{
spmixqr_control(
  nstart = 5L,
  maxit = 200L,
  tol = 1e-05,
  basis_type = c("tp", "gp", "mrf"),
  k = 20L,
  scale_coords = TRUE,
  label_order = c("slope", "intercept"),
  order_var = 1L,
  gate_ridge = 0.001,
  gate_maxit = 50L,
  gate_tol = 1e-08,
  sm_scale = 1,
  sm_floor = 0.001,
  coef_maxit = 50L,
  coef_tol = 1e-07,
  bandwidth = NULL,
  kde_grid = 512L,
  boot_B = 200L,
  boot_block = 4L,
  boot_areal_blocks = 8L,
  min_sites = 12L,
  lambda_error_default = 1,
  car_alpha_grid = c(0.5, 0.9, 0.95, 0.99),
  trace = FALSE,
  seed = NULL
)
}
\arguments{
\item{nstart}{Number of EM starts (the mixture likelihood is multimodal; Wu &
Yao 2016 stress initial-value sensitivity). The best start by penalised
smoothed objective is kept.}

\item{maxit, tol}{EM iteration cap and relative-change tolerance.}

\item{basis_type}{Spatial basis: \code{"tp"} (thin-plate), \code{"gp"} (low-rank Gaussian
process) for point data, or \code{"mrf"} (Markov random field) for areal data.}

\item{k}{Spatial-basis dimension (clamped to the number of unique
locations/regions minus one).}

\item{scale_coords}{Standardise coordinates before building the basis (kept as a
stored transform for prediction at new locations). Recommended \code{TRUE}.}

\item{label_order}{Component ordering key for relabelling: \code{"slope"} (default,
per-regime average ordering-covariate slope) or \code{"intercept"}.}

\item{order_var}{Index (among the slope covariates) used as the ordering
covariate; default \code{1} (the first non-intercept covariate).}

\item{gate_ridge}{Ridge on the non-spatial gate coefficients (also stabilises the
ICAR/MRF null space). Matches the \code{mixqrgate} default for the no-spatial reduction.}

\item{gate_maxit, gate_tol}{Inner gate Newton/IRLS controls.}

\item{sm_scale, sm_floor}{Smoothing-bandwidth tuning constant and floor for the
convolution-smoothed component M-step.}

\item{coef_maxit, coef_tol}{Inner penalised-Newton controls for the component step.}

\item{bandwidth, kde_grid}{Passed to \code{\link[mixqr:mixqr_control]{mixqr::mixqr_control()}} for the KDE density path.}

\item{boot_B, boot_block}{Bootstrap replicates and number of spatial blocks (per
axis) for the spatial-block bootstrap.}

\item{boot_areal_blocks}{Target number of contiguous blocks for the areal
(CAR) block bootstrap (connected-component / graph partition of \code{W}).}

\item{min_sites}{Minimum distinct locations required before a block bootstrap is
attempted (it under-covers with few blocks; Lahiri 2003).}

\item{lambda_error_default}{Default CAR penalty \code{lambda_phi} when \code{lambda_error}
is \code{NULL} (the CAR effect's roughness; select by BIC with \code{\link[=spmixqr_select]{spmixqr_select()}}).}

\item{car_alpha_grid}{Candidate proper-CAR \code{alpha} values for the optional coarse
profile in \code{\link[=spmixqr_select]{spmixqr_select()}} (alpha is fixed by default, not check-loss selected).}

\item{trace}{Logical; print EM progress.}

\item{seed}{Optional RNG seed (honoured throughout for reproducibility).}
}
\value{
A list of control parameters.
}
\description{
Tuning knobs for the penalised EM, the spatial basis, label handling, and the
bootstrap. All have sensible defaults.
}
