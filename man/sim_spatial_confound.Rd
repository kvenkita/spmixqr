% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_spatial_confound}
\alias{sim_spatial_confound}
\title{Simulate point data with spatial confounding (for Spatial+ / NNGP demos)}
\usage{
sim_spatial_confound(
  n = 400L,
  beta = 1,
  confound = 0.9,
  amp = 1.2,
  freq = 6,
  sd = 0.5,
  seed = NULL
)
}
\arguments{
\item{n}{Number of points.}

\item{beta}{True covariate slope (recovered by a deconfounded estimator).}

\item{confound}{Strength of covariate--space correlation (\code{0} = negative control).}

\item{amp}{Amplitude of the spatial field in the outcome.}

\item{freq}{Spatial frequency of the field (higher = finer; default 6).}

\item{sd}{Error scale.}

\item{seed}{Optional seed.}
}
\value{
A list with \code{data} (\code{y}, \code{x}), \code{coords} (\verb{n x 2}), and \code{truth} (\code{beta}, the
field \code{f}, \code{confound}).
}
\description{
Generates point-referenced data in which a covariate is correlated with a smooth
spatial field that also drives the outcome -- the spatial-confounding setting of
Dupont, Wood & Augustin (2022). A high-frequency field \code{f(s)} is used so the
covariate's spatial signal out-resolves a penalised spatial-error term (the
resolution gap that makes Spatial+ effective; Frisch--Waugh--Lovell). With
\code{confound = 0} it is a negative control (no confounding). The error is symmetric, so
the true \code{tau}-quantile slope equals \code{beta} at every \code{tau}.
}
\examples{
d <- sim_spatial_confound(n = 200, seed = 1)
str(d$truth)
}
\references{
Dupont, Wood & Augustin (2022, Biometrics).
}
