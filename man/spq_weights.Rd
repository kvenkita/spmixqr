% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{spq_weights}
\alias{spq_weights}
\title{Construct a spatial weights object for the CAR-error term}
\usage{
spq_weights(
  x,
  type = c("queen", "rook", "distance", "knn", "supplied"),
  d1 = 0,
  d2 = NULL,
  k = 5L,
  style = "B",
  ids = NULL,
  ...
)
}
\arguments{
\item{x}{The spatial object. For \code{type = "queen"}/\code{"rook"}: an \pkg{sf}/\code{sp}
polygon object, \strong{or} an \pkg{spdep} \code{nb}/\code{listw} (used directly). For
\code{type = "distance"}/\code{"knn"}: a two-column coordinate matrix/data frame. For
\code{type = "supplied"}: a square nonnegative matrix (\code{Matrix} or base).}

\item{type}{Weights construction: \code{"queen"}/\code{"rook"} (polygon contiguity),
\code{"distance"} (distance band \verb{[d1, d2]}), \code{"knn"} (k nearest neighbours,
symmetrised), or \code{"supplied"} (validate a user matrix).}

\item{d1, d2}{Lower / upper distance band (required for \code{"distance"}).}

\item{k}{Number of nearest neighbours (for \code{"knn"}).}

\item{style}{\pkg{spdep} weighting style; \code{"B"} (symmetric binary, default) is
required for a valid GMRF precision. \code{"W"} (row-standardised) is asymmetric and
triggers a warning (symmetrised before use).}

\item{ids}{Optional unit identifiers (row/column names of \code{W}).}

\item{...}{Currently unused.}
}
\value{
An object of class \code{spq_weights}: a list with \code{W} (sparse symmetric
\code{dgCMatrix}), \code{D} (sparse diagonal degree matrix), \code{ids}, \code{style}, \code{type},
\code{n_comp} (number of connected components, via \code{\link[spdep:compon]{spdep::n.comp.nb()}}), and
\code{n_island} (number of zero-degree units).
}
\description{
Builds a symmetric, nonnegative, sparse spatial weights matrix \code{W} and its degree
matrix \code{D} from a variety of inputs, wrapping the result in an \code{spq_weights} object
for use as the \code{spatial_W} argument of \code{\link[=spmixqr]{spmixqr()}}. The default style \code{"B"}
(symmetric binary) gives a valid Gaussian Markov random field precision base; the
row-standardised style \code{"W"} is asymmetric (lag-semantics) and warned against for
the CAR penalty.
}
\examples{
## supplied matrix (no spdep input needed)
Wm <- matrix(0, 4, 4); Wm[1,2] <- Wm[2,1] <- Wm[2,3] <- Wm[3,2] <- 1
Wm[3,4] <- Wm[4,3] <- 1
w <- spq_weights(Wm, type = "supplied")
w
}
\references{
Leroux et al. (2000); \pkg{spdep} (Bivand et al.).
}
